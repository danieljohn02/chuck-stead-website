# YouTube playlist auto-sync (server-side only — never exposed to client).
# When set, the Videos section auto-populates from this playlist.
# When unset, the site falls back to the hardcoded placeholder cards.

YOUTUBE_API_KEY=
YOUTUBE_PLAYLIST_ID=
